import React from 'react';

const Nav = () => (
  <header>
    <div className="logo">
    </div>
    <nav>
      <a href="#">Login</a>
      <a href="#">Sign Up</a>
    </nav>
  </header>
);

export default Nav;
